/**
 * VoiceNotes - AI Prompts 配置文件
 *
 * 本文件定义了应用中使用的各种 AI 提示词模板
 * 可以根据需要修改这些提示词来优化 AI 输出效果
 */

const PROMPTS = {
  /**
   * 去口语模式 - System Prompt
   *
   * 用途：将口语录音转写文本优化为规范的书面表达
   * 特点：作为 system prompt，指导 AI 的整体行为
   */
  REMOVE_FILLER_WORDS: {
    system: `你是一个专业的内容编辑，擅长将口语转写文本优化为规范的书面表达。

【任务目标】
去除文本中的冗余口语表达，保持原意不变，使表达更加专业、简洁、流畅。

【具体要求】
1. 去除无意义的填充词
   - 常见填充词：那个、呃、啊、嗯、然后然后、就是、就是那个
   - 重复表达：你知道吗、那个啥、怎么说呢

2. 优化句式结构
   - 调整为更符合书面语习惯的表达
   - 使用恰当的连接词，使句子更流畅
   - 避免过于随意的口语化表达

3. 保持内容完整性
   - 忠实于原文核心意思
   - 不添加原文中没有的内容
   - 不改变原文的观点和立场

4. 输出格式
   - 直接输出优化后的文本
   - 不添加任何解释或说明
   - 不使用 markdown 格式

【处理原则】
- 保守处理：如果某个词语可能是重要的，保留它
- 适度优化：保持一定的自然感，不要过度书面化
- 语境敏感：根据上下文判断是否需要保留某个表达`
  },

  /**
   * 生成纪要模式 - User Prompt
   *
   * 用途：将多条语音笔记整理成结构化的会议纪要
   * 特点：作为 user prompt，提供具体的任务指令
   */
  GENERATE_SUMMARY: {
    user: `请根据以下语音笔记生成结构化的会议纪要。

【输出格式】
# 会议纪要

## 一、核心要点
- 要点1
- 要点2
- 要点3

## 二、讨论内容
### 2.1 主题1
- 讨论内容

### 2.2 主题2
- 讨论内容

## 三、行动项
- [ ] 待办事项1 - 负责人 - 截止时间
- [ ] 待办事项2 - 负责人 - 截止时间

## 四、重要决策
1. 决策1
2. 决策2

## 五、下次讨论
- 议题1
- 议题2

---

【原始笔记】
{notes}

请严格按照以上格式生成纪要，确保内容完整、条理清晰。`
  }
};

/**
 * AI 模型配置
 */
const AI_CONFIG = {
  // 默认使用的模型
  defaultModel: 'qwen-plus',

  // 模型映射（可根据需要切换）
  models: {
    'qwen-plus': 'qwen-plus',           // 通义千问 Plus（推荐）
    'qwen-turbo': 'qwen-turbo',         // 通义千问 Turbo（快速）
    'qwen-max': 'qwen-max',             // 通义千问 Max（最强）
  }
};

/**
 * API 配置
 */
const API_CONFIG = {
  // 阿里云 DashScope API 基础 URL
  baseURL: 'https://dashscope.aliyuncs.com/compatible-mode/v1',

  // 超时时间（毫秒）
  timeout: 30000
};
